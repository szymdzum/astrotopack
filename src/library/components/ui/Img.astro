---
const { src, alt, ...rest } = Astro.props;
---

<img src={src} alt={alt} {...rest} />

<style lang="scss">
  img {
    display: block;
    width: 100%;
    height: auto;
    object-fit: cover;
  }
</style>
