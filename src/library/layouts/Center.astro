<div class="center">
  <slot />
</div>

<style lang="scss">
  .center {
    box-sizing: content-box;
    max-width: 60ch;
    margin-left: auto;
    margin-right: auto;
    padding-left: var(--space-s);
    padding-right: var(--space-s);
  }
</style>
