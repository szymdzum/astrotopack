---
import Logo from '@components/Logo.astro';
import Navigation from '@components/Navigation.astro';
import SocialLinks from '@components/SocialLinks.astro';
import logoImg from '/src/assets/images/puttopack_logo.png';

import { getEntry } from 'astro:content';
const content = await getEntry('landing', 'config');
const { pageName, menu } = content.data;
---

<header>
  <div>
    <Logo name={pageName} imgSrc={logoImg} />
    <Navigation menu={menu} />
    <SocialLinks />
  </div>
</header>

<style lang="scss">
  header {
    position: sticky;
    top: 0;
    z-index: 100;
    background: var(--background);
    border-bottom: var(--border-sm);

    div {
      display: flex;
      flex-wrap: wrap;
      align-items: baseline;
      margin-inline: auto;
      justify-content: space-between;
      max-width: var(--max-section-width);
      padding: 0 var(--padding-md) var(--padding-sm) var(--padding-md);
    }

    @media (max-width: 24em) {
      display: none;
    }
  }
</style>
